---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vue-app
  namespace: dad-group-27
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vue-app
  template:
    metadata:
      labels:
        app: vue-app
    spec:
      priorityClassName: high-priority
      containers:
        - name: web
          image: registry-172.22.21.115.sslip.io/dad-group-27/web:v4.0.4
          imagePullPolicy: Always
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "64Mi"
              cpu: "100m"

---
apiVersion: v1
kind: Service
metadata:
  name: vue-app
  namespace: dad-group-27
spec:
  ports:
    - port: 80
  selector:
    app: vue-app
